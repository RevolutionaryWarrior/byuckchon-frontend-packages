export type CheckboxTheme = {
  activate?: {
    box?: string;
    text?: string;
  };
  inactivate?: {
    box?: string;
    text?: string;
  };
  disabled?: {
    box?: string;
    text?: string;
  };
};
