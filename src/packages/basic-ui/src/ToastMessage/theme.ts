export type ToastMessageTheme = {
  default?: {
    bg?: string;
    text?: string;
  };
  error?: {
    bg?: string;
    text?: string;
  };
  success?: {
    bg?: string;
    text?: string;
  };
  warning?: {
    bg?: string;
    text?: string;
  };
};
