export const FormatData = () => {
  const formatPhoneNumber = (value: number) => {};

  const formatTelNumber = (value: number) => {};

  const formatBizNumber = (value: numbber) => {};

  const formatDate = (value: Date) => {};

  const maskName = (value: string) => {};

  const maskPhoneNumber = (value: number) => {};

  const commanizeData = (value: string | number) => {};

  const decommanizeData = (value: string | number) => {};
};
